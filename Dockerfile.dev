FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY frontend/package*.json ./frontend/
RUN cd frontend && npm install

# For development, we'll run concurrently via npm run dev
EXPOSE 5000 5173

CMD ["npm", "run", "dev"]
